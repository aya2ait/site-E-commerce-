<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navbar with Creative Search Bar</title>
    <!-- Inclure Bootstrap si nécessaire -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Styles pour la barre de recherche */
        .search-bar {
            display: none; /* Pour supprimer la barre de recherche */
        }

        .navbar {
            height: 80px; /* Augmenter la taille du navbar */
        }

        .navbar-brand {
            margin-right: auto;
            margin-top:20px; /* Placer le logo à droite */
        }

        .navbar-nav {
            margin-left: auto; /* Placer les autres éléments à gauche */
            margin-top:20px;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg bg-body-tertiary" style="background-color: #c6e2e9">
    <div class="container-fluid">
        <img src="{{ asset('image/logo1.png') }}" alt="Logo" width="12%" style="margin-left: 20px;" class="navbar-brand">

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item" style="margin-right: 20px; margin-left:550px">
                    <a class="nav-link active" aria-current="page" href="{{ path('home') }}" style="color: black; font-size: larger;">
                        <i class="fas fa-home"></i> Home
                    </a>
                </li>

                <li class="nav-item" style="margin-right: 20px;">
                    <a class="nav-link active" aria-current="page" href="{{path('user_order_list')}} "style="color: black; font-size: larger;">
                        <i class="fas fa-shopping-cart"></i> Mon panier
                    </a>
                </li>
                <li class="nav-item" style="margin-right: 20px;">
                    <a class="nav-link active" aria-current="page" href="{{path('app_categories')}} "style="color: black; font-size: larger;">
                        <i class="fas fa-shopping-cart"></i> produits
                    </a>
                </li>

                <li class="nav-item dropdown" style="margin-right: 20px;">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                       aria-expanded="false" style="color: black; font-size: larger;">
                        <i class="fas fa-user"></i> se connecter
                    </a>
                    <ul class="dropdown-menu">
                        {% if not app.user%}
                        <li><a class="dropdown-item" href="{{path('app_register')}}"
                               style="color: black; font-size: larger;">Sign up</a></li>
                        <li><a class="dropdown-item" href="{{path('app_login')}}"
                               style="color: black; font-size: larger;">Sign in</a></li>
                        {% else %}
                        <li><a class="dropdown-item" href="{{path('app_logout')}}"
                               style="color: black; font-size: larger;">logout</a></li>
                        {% endif %}
                    </ul>
                </li>
                {% if is_granted('ROLE_ADMIN')%}
                   
                   

                   <li class="nav-item dropdown" style="margin-right: 20px;">
                      <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                       aria-expanded="false" style="color: black; font-size: larger;">
                        <i class="fas fa-user"></i> ADMIN
                      </a>
                     <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{path('product_list')}}"
                               style="color: black; font-size: larger;">Products</a></li>
                        <li><a class="dropdown-item" href="{{path('product_store')}}"
                               style="color: black; font-size: larger;">add product</a></li>
                        <li><a class="dropdown-item" href="{{path('orders_list')}}"
                               style="color: black; font-size: larger;">Orders</a></li>
                       

                     </ul>
                   </li>

                {% endif %}

            </ul>
        </div>
    </div>
</nav>

</body>
</html>
